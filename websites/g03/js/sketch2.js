let myImages = [];


function preload(){
  // myImages[0] = loadImage('assets/screenshot/apps avalanche.png');
  // myImages[1] = loadImage('assets/screenshot/apps fantom.png');
}

function setup() {

  var canvas = createCanvas(windowWidth/2, windowHeight/2);
  background(255, 0, 200);

 // Move the canvas so it’s inside our <div id="sketch-holder">.
 canvas.parent('sketch-holder');



 // const container = select("#sketch-holder");
 //
 // for (let i = 0; i < myImages.length; i++) {
 // container.createImg(myImages[i], "DDD").position(i*10, i*10);
 // }

}

function draw() {
  // put drawing code here

}
